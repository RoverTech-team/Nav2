controller_manager:
  ros__parameters:
    update_rate: 100

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

    diff_drive_controller:
      type: diff_drive_controller/DiffDriveController

diff_drive_controller:
  ros__parameters:
    left_wheel_names:
      - base_front_left_wheel_joint
      - base_middle_left_wheel_joint  # removed from robot
      - base_back_left_wheel_joint
    right_wheel_names:
      - base_front_right_wheel_joint
      - base_middle_right_wheel_joint  # removed from robot
      - base_back_right_wheel_joint

    wheel_separation: 0.45
    wheel_radius: 0.1
    wheel_separation_multiplier: 1.3
    wheel_radius_multiplier: 1.0
    
    
    odom_frame_id: "odom"
    base_frame_id: "base_footprint"

    pose_covariance_diagonal: [0.001, 0.001, 0.001, 0.001, 0.001, 0.001]
    twist_covariance_diagonal: [0.001, 0.001, 0.001, 0.001, 0.001, 0.001]

    enable_odom_tf: false
    publish_rate: 100.0

    linear.x.max_velocity: 100000.0
    linear.x.min_velocity: -100000.0
    angular.z.max_velocity: 100000.0
    angular.z.min_velocity: -100000.0

    position_feedback: false
    use_stamped_vel: True
