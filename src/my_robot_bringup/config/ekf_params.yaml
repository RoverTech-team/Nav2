ekf_filter_node:
  ros__parameters:

    # ── Wheel odometry (differential: true → x,y,yaw pose become velocity deltas) ──
    odom0: /diff_drive_controller/odom
    odom0_config: [true, true, false, false, false, true,
                   false, false, false, false, false, false,
                   false, false, false]
    odom0_differential: true
    odom0_relative: false
    odom0_queue_size: 10

    # ── ZED IMU ──
    imu0: /zed/zed_node/imu/data
    imu0_config: [false, false, false, true, true, true,
                  false, false, false, false, false, true,
                  true, true, true]
    imu0_differential: false
    imu0_relative: false
    imu0_queue_size: 10
    imu0_remove_gravitational_acceleration: true

    # ── ArUco visual yaw ──
    pose0: /aruco/attitude
    pose0_config: [false, false, false, false, false, true,
                   false, false, false, false, false, false,
                   false, false, false]
    pose0_differential: false
    pose0_relative: false
    pose0_queue_size: 10

    # ── Frames ──
    map_frame: map
    odom_frame: odom
    base_link_frame: base_footprint
    world_frame: odom

    # ── General ──
    two_d_mode: true
    predict_to_current_time: true
    publish_tf: true
    print_diagnostics: true
    frequency: 30.0
    transform_time_offset: 0.0
    transform_timeout: 0.05
    debug: false
